# Base requirements - shared by all containers
requests            # HTTP library
python-dateutil     #
python-dotenv       # Environment variables
openai              # OpenAI API client
## CUDA 12.8 Enabled Bins
torch==2.7.0+cu128         # PyTorch with CUDA 12.8 support (from pip cache proxy)
torchaudio==2.7.0+cu128    # Audio processing with CUDA support
torchvision==0.22.0+cu128  # Computer vision with CUDA support (correct version for PyTorch 2.7.0)
transformers==4.44.2       # ML transformers (pinned for compatibility with PyTorch 2.7.0)

## Data Layer
asyncpg             # PostgreSQL async client
boto3               # AWS SDK, Data layer attachment storage
pyjson              # JSON utilities, MCP + More
beautifulsoup4      # HTML parsing, MCP + More
Cython              # C extensions
spacy==3.6.1        # NLP library

## Emotions & Observation
paho-mqtt>=1.6.0    # MQTT client for emotion publishing (MQTT 5.0 support)

## Chainlit-specific requirements
chainlit            # Core Requirement

## MCP 
mcp                 # MCP core
mcp-proxy           # Needed to proxy httpse to/from stdio
mcp-server-time     # MCP server for time
mcp-server-fetch    # MCP server for web fetching
mcp-server-git      # MCP server for git operations
wikipedia-mcp       # MCP server for Wikipedia

## Datalayer 
prisma              # Database ORM for datalayer
av>=12.0.0          # PyAV for audio processing

# Monitoring requirements
psutil==5.9.0
aiohttp>=3.11.11
#aiohttp==3.9.1
gputil==1.4.0
docker>=7.1.0

psycopg2-binary>=2.9.0